# Card Collector Inventory Manager — Python dependencies
# Install with:  pip install -r requirements.txt

# ── Core web app ──
Flask>=3.0
Flask-SQLAlchemy>=3.1
SQLAlchemy>=2.0
Werkzeug>=3.0
python-dotenv>=1.0

# ── Imaging ──
opencv-python-headless>=4.8      # NOTE: rapidocr below hard-depends on full opencv-python,
                                 # which pip installs as well and which needs libGL at import
                                 # time. On headless Linux: apt install libgl1 libglib2.0-0,
                                 # or see README "Requirements" for the pip-only swap.
Pillow>=10.0
numpy>=1.26
# These two carried NO constraint at all, so `pip install` would take a future major
# without anything to notice it. Floors match the versions the app is tested against
# (see requirements.lock); they are a floor and not a ceiling, so this still does not
# stop a major landing — requirements.lock is what makes an install reproducible.
rapidocr>=3.9
onnxruntime>=1.23
networkx>=2.5.1

# ── Reports & security/networking ──
reportlab>=4.0                   # PDF financial reports
cryptography>=42.0               # self-signed HTTPS certificate generation
requests>=2.31                   # provider / marketplace API calls

# ── Optional (features degrade gracefully if missing) ──
PyMuPDF>=1.24                    # optional: PDF import/rasterization  (import "fitz")
zeroconf>=0.130                  # optional: advertises the stable <name>.local address
# hnswlib>=0.8                   # optional: ANN shortlist for Search by Image on large
#                                # collections. Left commented out because PyPI has no
#                                # prebuilt wheels — installing it compiles C++ and would
#                                # fail a first install on machines without a toolchain.
#                                # Without it the search brute-forces with identical
#                                # ranking, just slower. Enable with: pip install hnswlib
